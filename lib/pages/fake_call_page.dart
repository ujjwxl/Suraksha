import 'package:flutter/material.dart';

class FakeCallPage extends StatelessWidget {
  const FakeCallPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Fake Call Page');
  }
}
